# 2026-05-02

## Documentation Audit & Refresh

Audited all project documentation against the current codebase and made updates to reflect the current implementation.

### Changes Made

**Application Details.md**
- Added simple mermaid architecture diagram (10 nodes) showing the two UI paths (Tk → direct orchestrator calls; Web → HTTP → API endpoints)
- Replaced stale `src/backtesting.py` section with `src/orchestrator/common/backtesting.py`
- Replaced stale `src/edinet_api.py` section with `src/orchestrator/common/edinet.py`
- Replaced stale `src/data_processing.py` and `src/orchestrator/processor` sections with actual modules: `src/orchestrator/common/db_config.py`, `ratios.py`, `sqlite.py`, `validation.py`
- Updated test file listing to match actual test files (added 6, removed 1 stale)
- Updated "Current project status" to mention web workstation
- Updated last-updated date to 2026-05-02

**Frontend Architecture.md**
- Added simple mermaid dataflow diagram (9 nodes) showing pages → JS modules → API endpoints, with dotted import lines for shared common modules
- Fixed directory layout: removed non-existent `pages/` dir, added `api/screening.py`
- Updated screening module from "stub" to fully documented with feature list
- Added `api/screening.py` documentation with full endpoint listing
- Updated `api/__init__.py` description to mention screening route extension
- Added design conventions for `resolveDbPath` and File System Access API
- Fixed "Adding a new screen" step 4 with concrete API router extension code

**RUNNING.md**
- Updated frontend description from "single-page app" to "multi-page vanilla JS application"
- Corrected stub status: Screening is fully functional, Security Analysis is still a stub

### Files NOT changed (already accurate)
- **Readme.md** — up to date, no stale references to removed steps
- **CHANGELOG.md** — correctly documents removed steps; historical entries accurate
- **LOGGING.md** — accurate, no stale references
- **Contributing.md** — accurate
- **Feature Development docs** — marked as COMPLETED/historical, correct for archival reference

### Key findings during audit
- `src/data_processing.py` and `src/edinet_api.py` no longer exist; functionality moved to `src/orchestrator/common/`
- `src/web_app/frontend/pages/` directory referenced in docs does not exist; pages are flat directories
- The web screening `index.js` is ~1600 lines of fully-implemented criteria builder (not a stub)
- Browser `resolveDbPath` utility resolves relative DB paths against server's `repo_root`
- File uploads use base64 encoding + server-side temp file resolution via `resolve_file_uploads`
